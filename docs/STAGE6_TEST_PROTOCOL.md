# 冻结后的完整测试集评估

2026-09-17，用户明确要求比较三种方法在整个测试集的表现，不选择特定片段。R2已完成两轮训练并冻结，本轮授权首次对test01做完整模型评估；不再用测试结果训练、调参或重新选择checkpoint。

- 主要方法：`stage5_R2_seed42/best.pth`加LP=0.3；用户确认的生产`adaptive_analytical_manus_wuji_hand_2_right.yaml`、LP=0.3；SDK2026.8.31内置配置和状态。
- 原始GeoRT在完整测试集作为历史附录保留；R2/Wuji原始无滤波输出仅用于区分滤波影响。
- test01全部9段、20,989条有效读取，0条因质量检查被丢弃；保留重复读数，原顺序运行，片段起点独立reset。没有看结果后选择动作、帧或重采样。
- 所有姿态均计算共同精确FK。主要方法及原始GeoRT的自碰撞逐条计算，覆盖100%，不沿用验证集的1800条抽检。
- 主要报告全读数合并统计，同时提供9段等权平均、P95及每段完整明细。方向逐指；开口使用训练时固定尺度，统计人手间距≥15mm的全部有效关系。<15mm另报site间距，不当真实接触误差。
- 局部响应在每段内部取相邻读数，再合并全部9段。报告输入位移≥0.1mm的所有运动，以及0.5–10mm幅度的微调；同时报告低响应、增益偏离1、输入<0.1mm时输出位移。不存在采集时钟，不换算速度、加速度或延迟。
- 不重新拟合归一化、关系尺度、个人手型或滤波参数。不把SDK内部滤波未知伪装成完全相同参数的算法比较。

执行前的机器可读冻结协议、源代码、模型/配置/资产标识保存在`reports/stage6/test_full/protocol.json`与`evaluator_source.py`。每段原始输出、生产Wuji缓存、完整碰撞深度和最终JSON报告均保留，不覆盖历史评估。

执行入口：

```bash
conda activate geort
cd /home/xense-wufan/GeoRT
python -m geort.evaluate_full_test --output reports/stage6/test_full
```

已存在的目录会被拒绝覆盖。计算中断时`--measure-only`仅恢复冻结输出的几何统计，不再次选择模型；完成后拒绝重新写结果。最终结果是单操作者、同标定条件下的独立采集会话结果，不能外推到其他操作者或物理接触成功率。
