# GeoRT 重定向开发约定

- 每轮重定向开发或实验开始前，回顾 `docs/DEVELOPMENT_GOALS.md` 的相关目标、范围和验收要求，再阅读本轮涉及的代码及最新阶段报告。该文件是用户提供的开发目标原文；后续明确指令优先。
- 目标是保留任务相关方向、多指关系和精细可控性，在此基础上选择自然、连续、可预测的构型；不能把消除反弯或保持父模型指尖位置单独作为完成标准。
- 区分输入信息、全手上下文与 loss 的收益。离线选解/预训练是实验手段，不取代骨骼信息、协调能力和操作质量的主线验证。
- 后续实验使用 seed=42，保留旧实验原有种子与结果。
- 后续效果对照必须包含：原始 GeoRT、候选 GeoRT、用户确认的 wuji-retargeting。配置与边界见 `docs/WUJI_BASELINE.md`；缺少外部对照的报告不能作为新一轮完整验收依据。
- 默认模型、旧输入和旧 checkpoint 保持兼容。新模型、损失和修正显式启用；不以 smoke 通过宣称质量改进。
- 每次修改后提供可执行的预览命令。报告代码、本地验证、实机验证的实际边界。
- 不自动连接或驱动实机，不自动 git push，不覆盖既有数据或 checkpoint。
